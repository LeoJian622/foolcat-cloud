spring:
  profiles:
    active: test
---
server:
  port: 8008
spring:
  profiles: test  #运行环境 + 命名空间名称
  application:
    name: foolcat-cloud-gateway   #唯一名称
  cloud:
    nacos:
      discovery:
        server-addr: nacos.foolcat.xyz:8848  #Nacos 链接地址
        namespace: e23b1149-8fa2-4d6e-aa65-00ce8d78d266 #Nacos 命名空间ID
    #      config:
    #        server-addr: nacos.foolcat.xyz:8848  #Nacos 链接地址
    #        namespace: e23b1149-8fa2-4d6e-aa65-00ce8d78d266 #Nacos 命名空间ID
    #        group: DEFAULT_GROUP  # 默认分组就是DEFAULT_GROUP，如果使用默认分组可以不配置
    #        file-extension: yml   #默认properties
    gateway:
      discovery:
        locator:
          enabled: true    #表明gateway开启服务注册和发现的功能，并且spring cloud gateway自动根据服务发现为每一个服务创建了一个router，这个router将以服务名开头的请求路径转发到对应的服务
          lower-case-service-id: true  #是将请求路径上的服务名配置为小写（因为服务注册的时候，向注册中心注册时将服务名转成大写的了
      # 路由数组[路由 就是指定当请求满足什么条件的时候转到哪个微服务]
      routes:
        - id: admin-server-router             # 当前路由的标识, 要求唯一
          uri: lb://admin-service       # lb指的是从 nacos 中按照名称获取微服务,并遵循负载均衡策略
          predicates: # 断言(就是路由转发要满足的条件)
            - Path=/admin/**             # 当请求路径满足Path指定的规则时,才进行路由转发
          filters: # 过滤器,请求在传递过程中可以通过过滤器对其进行一定的修改
            - StripPrefix=1              # 转发之前去掉1层路径
---
server:
  port: 8008
spring:
  profiles: pre  #运行环境 + 命名空间名称
  application:
    name: foolcat-cloud-gateway   #唯一名称
  cloud:
    nacos:
      discovery:
        server-addr: nacos.foolcat.xyz:8848  #Nacos 链接地址
        namespace: 56eb50fa-9522-4130-abe2-49710c1d47b3 #Nacos 命名空间ID
      config:
        server-addr: nacos.foolcat.xyz:8848  #Nacos 链接地址
        namespace: 56eb50fa-9522-4130-abe2-49710c1d47b3 #Nacos 命名空间ID
        group: DEFAULT_GROUP  # 默认分组就是DEFAULT_GROUP，如果使用默认分组可以不配置
        file-extension: yml   #默认properties
    gateway:
      discovery:
        locator:
          enabled: true    #表明gateway开启服务注册和发现的功能，并且spring cloud gateway自动根据服务发现为每一个服务创建了一个router，这个router将以服务名开头的请求路径转发到对应的服务
          lower-case-service-id: true  #是将请求路径上的服务名配置为小写（因为服务注册的时候，向注册中心注册时将服务名转成大写的了
---
server:
  port: 8008
spring:
  profiles: prd  #运行环境 + 命名空间名称
  application:
    name: foolcat-cloud-gateway   #唯一名称
  cloud:
    nacos:
      discovery:
        server-addr: nacos.foolcat.xyz:8848  #Nacos 链接地址
        namespace: 766bc0e2-b00b-47ad-b041-ed4f729ccb8b #Nacos 命名空间ID
      config:
        server-addr: nacos.foolcat.xyz:8848  #Nacos 链接地址
        namespace: 766bc0e2-b00b-47ad-b041-ed4f729ccb8b #Nacos 命名空间ID
        group: DEFAULT_GROUP  # 默认分组就是DEFAULT_GROUP，如果使用默认分组可以不配置
        file-extension: yml   #默认properties
    gateway:
      discovery:
        locator:
          enabled: true    #表明gateway开启服务注册和发现的功能，并且spring cloud gateway自动根据服务发现为每一个服务创建了一个router，这个router将以服务名开头的请求路径转发到对应的服务
          lower-case-service-id: true  #是将请求路径上的服务名配置为小写（因为服务注册的时候，向注册中心注册时将服务名转成大写的了
